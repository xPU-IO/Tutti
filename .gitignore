# Build directory
build/
build/*
build_tutti/
build_tutti/*
build_layer2/
build_layer2/*
# In-tree CMake build dir under the Layer 2 tests (configured artifacts only).
tutti/device_manager/tests/build/
build_snvme_module/

# Third-party packages directory
third_pkgs/
third_pkgs/*

# Reference / scratch upstream sources (e.g. linux kernel + nvme drivers
# used as a baseline for snvme port work).  These are large, redistributed
# upstream trees -- do not commit.
temp/

.venv/

# IDE specific files
.vscode/
.idea/
.ccls-cache/
*.swp
*~
.clangd
.cache
db/
temp/
temp/*
# CodeBuddy / IDE assistant local state -- per-developer, not source.
.codebuddy/

# Smoke / stress test runtime logs (written next to the binary or repo
# root; regenerated on every run).
*.log
kv_cache_e2e_stress.log

# CMake user presets are per-developer (toolchain/CUDA paths etc.).
# Commit CMakePresets.json deliberately if you ever need a shared baseline.
CMakeUserPresets.json
CMakePresets.json
build_dependencies.tsv

# Smoke-test binaries built in-tree by the test/Makefile.  The .c/.cu
# sources are committed; the binaries are not.
backends/local/kernel_modules/test/snvme_smoke
backends/local/kernel_modules/test/snvme_smoke_gpu
backends/local/kernel_modules/test/snvme_smoke_addq
backends/local/kernel_modules/test/snvme_smoke_qgroup
backends/local/kernel_modules/test/snvme_smoke_recycle
backends/local/kernel_modules/test/snvme_smoke_io
tutti/device_manager/nvme/kernel_modules/test/snvme_smoke
tutti/device_manager/nvme/kernel_modules/test/snvme_smoke_gpu
tutti/device_manager/nvme/kernel_modules/test/snvme_smoke_addq
tutti/device_manager/nvme/kernel_modules/test/snvme_smoke_qgroup
tutti/device_manager/nvme/kernel_modules/test/snvme_smoke_recycle
tutti/device_manager/nvme/kernel_modules/test/snvme_smoke_io
tutti/device_manager/nvme/kernel_modules/test/snvme_ubind

# Nsight Systems profile output -- per-run, not source.
*.nsys-rep
*.sqlite

# Stress test sweep logs + stdout captures (per-run, not source)
sweep_logs/

# Exported chat history (not source)
history_*.md
tutti_build/

# Local CUDA-like profile verification build trees.
tutti/build-profile-cuda/
tutti/build-profile-host/
tutti/build-profile-cuda/
tutti/build-profile-host/

# Phase 3 gate script working build trees (scripts/phase3_gate.sh).
tutti/build_host/
tutti/build_cuda/

/chat/

# Kernel module build artifacts (out-of-tree kbuild debris).
Module.symvers
modules.order
*.o
*.ko
*.mod
*.mod.c
